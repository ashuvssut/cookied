// dummy apis
// TODO: create separate useRestBmShelfDB hook in which these dummy apis will be kept. The hook will return the dummy apis which will be used in HomeScreen.tsx to get the dummy apis and use them there.
// The main motive of the useRestBmShelfDB hook:
// 1. use useQuery hook in useRestBmShelfDB hook and return useQuery's loading and error states from the useRestBmShelfDB hook to use them in any screen.
// 2. use jotai's LoadingModal useAtom
// 3. use React Native SnackBar from this hook only to show error messages

// REST CRUD | FOLDERS

// REST CRUD | BOOKMARKS
